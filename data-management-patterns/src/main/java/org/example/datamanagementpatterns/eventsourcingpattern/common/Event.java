package org.example.datamanagementpatterns.eventsourcingpattern.common;

public interface Event {
    // Marker interface for events
    // You can add common methods for events if needed (e.g., getEventType(), getEventTimestamp())
}
